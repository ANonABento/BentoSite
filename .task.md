# Perf 2.1b — Bundle size audit + code split (bentosite)

Same as Perf 2.1 but for bentosite. Smaller surface (portfolio site), so wins may be smaller. Still worth doing — physics-based BentoGrid + Three.js + chat panel are likely heavy.

## Phases
1. Baseline via `@next/bundle-analyzer`. Save to `docs/perf/bundle-baseline-2026-05-04.md`.
2. Top suspects: Matter.js (physics), Three.js (3D viewer), the chat panel. Verify they're dynamic-imported per route — should NOT ship on `/playground/2048` if they're not used there.
3. Apply optimizations: dynamic imports for heavy 3D / physics, route-level code-splitting.
4. Re-measure.

Coordinate with: PRs #66, #67, #68 + bento-ya tasks T4, T5, T7 in flight on bentosite. Use a dedicated worktree.

Target: ≥20% per-route reduction.

## Acceptance criteria
Same as Perf 2.1, adapted for bentosite.

## Commit + PR
- ONE conventional commit: `perf(bundle): bentosite code-splitting + dead-code elimination`
- Body: route delta table.
- **NO AI attribution footer**.
- Do NOT merge.

## Context
- Workspace: bentoSite
- Branch: bentoya/perf-2-1b-bundle-size-audit-code-split-bentosite
- Working dir: /Users/bentomac/bentosite/.worktrees/bentoya-b6d20022-8181-4eb3-b7f8-53e11e0bbe59

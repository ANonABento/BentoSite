# Contact form with email send via API route

Add /contact route with form (name, email, message). POST to /api/contact which sends via Resend or similar (use env vars only — fail gracefully if not configured). Honeypot for spam. Acceptance: form validates client-side, API route exists, "Message sent" toast on success, npm run type-check passes.

## Context
- Workspace: bentoSite
- Branch: bentoya/contact-form-with-email-send-via-api-route
- Working dir: /Users/bentomac/conductor/repos/bentosite/.worktrees/bentoya-1c760c87-8b3e-41ba-ac3e-f13e638c5423

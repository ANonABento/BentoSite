# CI B.8 — CI speedup + caching pass (bentosite)

Same as B.7 but for bentosite. Already has parallel jobs (lint-and-type-check + unit-tests + security-audit) so the gain is smaller — focus on caching + skip-unchanged-paths.

## Phases
1. Profile current runs
2. Add `actions/cache` for `.next/cache`, vitest cache
3. Add `paths` filters (skip CI on docs-only changes if appropriate)
4. Verify timings improve

Target: <3min per PR.

Document in `docs/ci-perf.md`.

## Out of scope / Commit
Same pattern as B.7.

## Commit + PR
- `ci: bentosite caching + path filters`
- **NO AI attribution footer**.
- Do NOT merge.

## Context
- Workspace: bentoSite
- Branch: bentoya/ci-b-8-ci-speedup-caching-pass-bentosite
- Working dir: /Users/bentomac/bentosite/.worktrees/bentoya-e883b6ea-3470-4ba2-b72c-6836211e82a4

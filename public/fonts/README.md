# bentOS Font Candidates

Drop local webfont candidates here while iterating:

- `bentos-logo.woff2` for the main `bentOS` mark.
- `bentos-terminal.woff2` for boot chrome, loading labels, footer text, and prompts.

Preview candidates at `/boot-font-lab.html` before promoting them into the app.

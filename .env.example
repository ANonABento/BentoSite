# bentosite Environment Variables
# Copy this file to .env.local and fill in your values

# =============================================================================
# REQUIRED FOR CHAT FUNCTIONALITY
# =============================================================================

# Google Generative AI API Key (for Gemini 1.5 Flash)
# Get your key at: https://makersuite.google.com/app/apikey
# Without this key, the chat will run in demo mode with fallback responses
GOOGLE_GENERATIVE_AI_API_KEY=

# =============================================================================
# OPTIONAL CONFIGURATION
# =============================================================================

# Feedback storage file path
# Defaults to system temp directory if not set
# For production, consider using a database instead of file storage
# FEEDBACK_FILE=/path/to/feedback.json

# Contact form email delivery via Resend
# If these are omitted, /api/contact accepts valid messages without sending email.
RESEND_API_KEY=
CONTACT_FROM_EMAIL=
CONTACT_TO_EMAIL=k69jiang@uwaterloo.ca

# =============================================================================
# NOTES
# =============================================================================
#
# Demo Mode:
#   If GOOGLE_GENERATIVE_AI_API_KEY is not set, the chat API will return
#   isDemoMode: true and provide fallback responses with contact info.
#
# Feedback Storage:
#   The current file-based storage is suitable for development/demo.
#   For production, implement a database solution (Supabase, PostgreSQL, etc.)
#   The feedback is automatically capped at 1000 entries to prevent unbounded growth.
